# Bootstrap v10
1. Read AGENTS.md, V10_CANONICAL_SPEC.md, POLICY.md and MIGRATION.md.
2. Find the host's current task. Never silently relabel a v9 task as v10.
3. Read .ai-workflow/PROJECT_PROFILE.json if adopted; the example is not a live binding.
4. Read .workflow-local/current-task.json, contract lock, evidence and review.
   Missing task: Lead writes a short contract from Owner intent. Missing capabilities:
   record WAITING_CAPABILITY rather than asking Owner to transfer technical packets.
5. Resume the recorded feature on its branch; inspect work before changing anything.
6. Perform LEAD, IMPLEMENTER or REVIEWER responsibilities from the current packet.
   Reviewer must not share the implementation session.
7. Use scripts/workflow.mjs help for packet tooling. Do not invoke legacy scripts.
8. Return short Vietnamese progress and a concrete user test when ready.

ChatGPT web can clarify product intent. Codex local is the default execution contact.
No native CLI command/model version is assumed by this kit.
